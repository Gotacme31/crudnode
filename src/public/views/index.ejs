<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CRUD NODE</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.1/css/all.css" />

</head>
<body>
    <div class="container-xl mt-4">
            <h2 class="text-center text-danger">Pokemons CRUD</h2>
            <div class="row">
            <div class="col-md-3">
                <div class="card">
                    <div class="cord-body">
                        <form action="/add" method="post">
                            <div class="form-group">
                                <input type="text" name="name" placeholder="Name" class="form-control mb-3" autofocus required>
                            </div>
                            <div class="form-group">
                                <input type="text" name="type" placeholder="Type" class="form-control mb-3 " required>
                            </div>
                            <div class="form-group">
                                <input type="text" name="gen" placeholder="Generation" class="form-control mb-3 " required>
                            </div>
                            <button type="submit" class="btn btn-primary">Save new pokemon</button>
                        </form>
                    </div>
                </div>
            </div>
            <div class="col-md-9">
                <table class="table table-bordered table-hover">
                    <thead>
                        <tr>
                            <td>Name</td>
                            <td>Type</td>
                            <td>Generation</td>
                            <td></td>
                    </tr>
                    </thead>
                    <tbody>
                        <% if(dataPoke){ %>
                            <% for(let i=0; i < dataPoke.length; i++) {%>
                                <tr>
                                    <td><%= dataPoke [i].name%></td>
                                    <td><%= dataPoke [i].type%></td>
                                    <td><%= dataPoke [i].gen%></td>
                                    <td>
                                        <a href="/delete/<%= dataPoke [i].id_pokemon %>" class="btn btn-danger"><i class="fas fa-trash-alt"></i></a>
                                        <a href="/update/<%= dataPoke [i].id_pokemon %>" class="btn btn-warning"><i class="fas fa-edit"></i></a>
                                    </td>
                                </tr>
                            <% } %>
                        <% } %>
                    </tbody>
                </table>
            </div>  
            
    </div>
</body>
</html>